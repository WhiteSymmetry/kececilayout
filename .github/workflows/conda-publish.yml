name: Publish to Anaconda Cloud

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Miniconda
        uses: conda-incubator/setup-miniconda@v3
        with:
          miniconda-version: "23.11.0-2"  # Daha eski bir versiyon
          python-version: "3.10"  # Python 3.10 kullanıyoruz
          auto-activate-base: true
          channels: conda-forge,defaults
          channel-priority: strict
      
      - name: Prepare build environment
        run: |
          conda config --set always_yes true
          conda config --set changeps1 false
          conda update -n base -c conda-forge conda conda-build --all
          conda install -c conda-forge "python=3.10" "conda-build=24.1.2" "conda-verify=3.4.2" anaconda-client -y
          conda clean --all -y
      
      - name: Build Conda package
        run: |
          mkdir -p ./conda_pkgs
          conda build . \
            --output-folder ./conda_pkgs \
            --python 3.10 \
            --numpy 1.21 \
            --no-test
      
      - name: Convert to .conda format
        run: |
          conda install -c conda-forge conda-package-handling -y
          find ./conda_pkgs -name "*.tar.bz2" -exec bash -c 'cph convert "{}" "$(dirname "{}")/$(basename "{}" .tar.bz2).conda"' \;
      
      - name: Upload to Anaconda Cloud
        env:
          ANACONDA_TOKEN: ${{ secrets.ANACONDA_TOKEN }}
        run: |
          anaconda -t $ANACONDA_TOKEN upload \
            ./conda_pkgs/**/*.conda \
            --label main \
            --skip-existing
